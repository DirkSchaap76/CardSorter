package program;

import program.Start;

import cards.Card;

public class Menu {
	Start start = new Start();

	while(true)
	{
		int option = start.scanner.nextInt();

		switch (option) {
		case 1:
			/*
			 * test goal: press 1 to create a new card enter color (string, scanner) enter
			 * value (int, scanner)
			 * 
			 * note to self: might need tostring for value
			 */

			break;
		case 2:
			break;
		default:
			break;
		}
	}
	

	}
}
